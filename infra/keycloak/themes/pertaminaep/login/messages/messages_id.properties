# Overrides for Keycloak's own bundled Indonesian translations (enabled
# via defaultLocale: "id" in realm-smart-cctv-ai.json) — only the specific
# keys flagged as reading oddly to a real Indonesian speaker. Everything
# else still comes from Keycloak's built-in id bundle.
usernameOrEmail=Username atau Email
